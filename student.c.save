#include <stdio.h>



// student structure
//struct Student...
struct Student {
	char firstname[50];
	char lastname[50];
	int age;
	int studentid;
};

void printStudent(struct Student* student)
{
printf("%s, %s, age: %f, ID: %f", firstname, lastname, age, studentid)
}


void printAllStudents(struct Student students[], int num)
{
  // call printStudent for each student in students
}


int main()
{

  // an array of students
  //xxx students;
char students[numStudents];
  int numStudents = 0;
  while (1)
  {
    char c;
    printf("\nEnter a to add, p to print, q to quit:");
    fgets(input, 256, stdin);
    if (sscanf(input, "%c", &c) != 1) continue;
    if (c == 'q') 
    {
      break;
    }
    else if (c == 'p')
    {
      // print all students
      printAllStudents(students, numStudents);
      continue;
    }
    else if (c == 'a')
    {
      // enter a new student
char input[256], firstname[256], lastname[256};
int age, studentid;
printf("First name?\n");
 fgets(input, 256, stdin);
sscanf(input, "s", firstname);
printf("Last Name?\n");
fgets(input, 256, stdin);
sscanf(input,"s", lastname);
while (1)
{
printf("Age?\n");
fgets(input, 256, stdin);
if (sscanf(input, "d", &age) ==1) break;
printf("Not Valid, try again!\n");
}
printf("ID?\n")
while(1)
{
fgets(input, 256, stdin);
if (sscanf(input, "d", &studentid) ==1) break;
printf("Not Valid, try again!\n");
} 
 numStudents++;
struct Student numStudents;
strcpy(numStudents.firstname, firstname);
strcpy(numStudents.firstname, firstname);
strcpy(numStudents.firstname, firstname);
strcpy(numStudents.firstname, firstname);



   }
  }
  
  printf("\nGoodbye!\n");
}
